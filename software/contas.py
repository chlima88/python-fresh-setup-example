def soma(numA: int, numB: int) -> int:
    """
    Sum numA and numB. Returns the result
    @params:
    numA: int
    numB: int
    @return: int
    """
    result = numA + numB
    return result


soma(1, 2)
